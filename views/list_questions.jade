.content
  section

    .row
      - if (flash.error != null){
        .span16
          .alert-message.error
            = flash.error
      -} else if (flash.success != null){
        .span16
          .alert-message.success
            = flash.success
      -}
      .hero-unit
        .row
          .span8
            h3 Déjà <span class="label warning">#{questions.length}</span> instants de bonheur, <span class="label important">#{answers}</span> commentaires et  <span class="label success">#{votes}</span> votes - Pourquoi ne pas ajouter votre contribution, soit en commentant à un bonheur ci-dessous soit en partant un nouvel instant ou encore en votant?
          .span6
            <br/>
            <center><a class='btn primary large' style="font-size:24px" href= '/question'> Partager un nouvel <br/>instant <br/>de bonheur ?</a></center>


      each question in questions
        .span16
          h2.question <a href="/question/#{question._id}/show">#{question.body}</a>
          - var reponse = 'avec ' + question.answers.length + ' commentaires'

          if(question.answers.length==0)
            reponse = 'sans aucun commentaire pour l\'instant'
          else if(question.answers.length==1)
            reponse = 'avec un seul commentaire pour l\'instant'
          - var vote = 'vote'
          if(question.votes>0)
            vote = vote+'s'
          p <small>partagé le #{question.date.getDate()}/#{question.date.getMonth()+1}/#{question.date.getFullYear()} par #{question.author} #{reponse} et #{question.votes} #{vote}.</small>
      


